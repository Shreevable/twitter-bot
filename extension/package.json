{
  "name": "twitter-video-dubber-extension",
  "version": "1.0.0",
  "description": "Chrome extension for Twitter video dubbing",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:version": "VERSION=$npm_config_version node build.js",
    "clean": "rm -rf build dist"
  },
  "dependencies": {
    "archiver": "^5.3.1"
  }
}
